# Shared PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Shared JWT secret (used by users-service and builds-service)
JWT_SECRET=changeme
